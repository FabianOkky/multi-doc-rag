<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!--
            Database: PostgreSQL + pgvector, never SQLite — vector search needs the real thing.
            The connection details come from .env (or .env.testing when you keep one), but the
            search_path is pinned to an isolated "testing" schema right here so it cannot be
            overridden by accident. RefreshDatabase only drops the tables inside the connection's
            search_path, so the suite can share a database with development and still never touch
            development data. AppServiceProvider creates that schema on connect.
        -->
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_SEARCH_PATH" value="testing"/>
        <env name="MAIL_MAILER" value="array"/>
        <!--
            Cross-lingual retrieval costs one QueryTranslationAgent prompt per distinct
            question. It stays off for the suite as a whole so no test accidentally
            reaches a provider; BilingualRetrievalTest turns it back on explicitly,
            with the agent faked.
        -->
        <env name="RAG_MULTILINGUAL_QUERY" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
